{
  "name": "teap-quiz",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix server install && npm --prefix server run build",
    "start": "npm --prefix server run start"
  },
  "engines": {
    "node": "22.x"
  }
}
